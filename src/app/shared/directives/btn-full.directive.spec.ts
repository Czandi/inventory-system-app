import { BtnFullDirective } from './btn-full.directive';

describe('BtnFullDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnFullDirective();
    expect(directive).toBeTruthy();
  });
});
