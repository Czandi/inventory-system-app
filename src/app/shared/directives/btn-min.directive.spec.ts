import { BtnMinDirective } from './btn-min.directive';

describe('BtnMinDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnMinDirective();
    expect(directive).toBeTruthy();
  });
});
